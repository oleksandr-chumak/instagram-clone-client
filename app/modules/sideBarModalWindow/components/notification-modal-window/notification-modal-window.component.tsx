import { FC } from 'react';
import { SideBarModalWindowContainer } from '../../features';

export const NotificationModalWindow: FC = () => {
  return (
    <SideBarModalWindowContainer >
      Notification
    </SideBarModalWindowContainer>
  );
};
